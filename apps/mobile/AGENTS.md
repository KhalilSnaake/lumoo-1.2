# Expo HAS CHANGED

Le projet est sur **Expo SDK 54** (voir `apps/mobile/package.json`).
Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
Ne PAS suivre les docs d'une autre version d'Expo.
